name: build
on:
  pull_request: {}
  workflow_dispatch: {}
  merge_group: {}
permissions: {}
jobs:
  build:
    runs-on: ubuntu-24.04-arm
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 0
      - name: Validate JSON
        run: jq empty .agents/plugins/marketplace.json .release-please-manifest.json release-please-config.json plugins/locus/plugin.json plugins/locus/.codex-plugin/plugin.json plugins/iter/plugin.json plugins/iter/.codex-plugin/plugin.json
      - name: Check whitespace
        env:
          EVENT_NAME: ${{ github.event_name }}
          PULL_REQUEST_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          PULL_REQUEST_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          MERGE_GROUP_BASE_SHA: ${{ github.event.merge_group.base_sha }}
          MERGE_GROUP_HEAD_SHA: ${{ github.event.merge_group.head_sha }}
        run: |-
          case "$EVENT_NAME" in
            pull_request)
              git diff --check "$PULL_REQUEST_BASE_SHA...$PULL_REQUEST_HEAD_SHA"
              ;;
            merge_group)
              git diff --check "$MERGE_GROUP_BASE_SHA...$MERGE_GROUP_HEAD_SHA"
              ;;
            workflow_dispatch)
              git show --check --format= HEAD
              ;;
          esac
